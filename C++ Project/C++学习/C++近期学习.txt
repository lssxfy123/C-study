
2. 看之前苹果系统上的Observer观察者，依赖注入
3. 将消息服务器中多线程的循环标志改成voliate  http://blog.chinaunix.net/uid-10666692-id-1759595.html，自己写个程序测试下。

volatile是保证编译器不对代码进行优化，每次读取变量时从内存中读取，而不是读取寄存器，
加了volatile一定不优化，但如果不加，也不一定就会优化，有可能优化，也有可能不优化。